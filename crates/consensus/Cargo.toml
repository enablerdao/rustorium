[package]
name = "rustorium-consensus"
version = "0.1.0"
edition = "2021"

[dependencies]
gluon-consensus = "0.1"
tendermint = "0.30"

tokio = { version = "1.0", features = ["full"] }
anyhow = "1.0"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"
